{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["generate"],
      "outputs": ["dist/**"]
    },
    "generate": {},
    "lint": {},
    "migrate": {},
    "start:users": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
